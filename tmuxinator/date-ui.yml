# /home/adrian/.config/tmuxinator/date-ui.yml

name: superportero
root: ~/

windows:
  - editor:
      layout: main-vertical
      panes:
        - nvim -S ~/.local/share/nvim/session/date-ui
        - cd ~/Workspace/date-ui; npm start
